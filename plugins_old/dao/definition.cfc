this is the file that gets called by coldspring - 
defines custom dependenies through init such as a DATASOURCE for the DAO plugin
defines dependencies to other plugins
defines custom metadata (cfproperty attributes) that it uses